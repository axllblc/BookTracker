{% extends 'base.html.twig' %}

{% block title %}BookTracker - ContactController
{% endblock %}

{% block body %}
    <div class="container">
        {% for message in app.flashes('sucess') %}
            <div class="alert alert-sucess mt-4">
                {{ message }}
            </div>
        {% endfor %}
        <h1>Formulaire de contact</h1>

        {{ form(form) }}
        </div>
    {% endblock %}
